---
title: Monica AI 数据表关系
description: Monica AI 已有基础表与新增业务表的数据流向、关系图和权限边界说明
date: 2026-06-11
---

## 1. 目的

本文用于在落地 Prisma schema 前，统一说明当前已有基础数据表和 Monica AI 新增业务表之间的数据流向。

重点回答：

- 每张表对应哪个中文业务概念。
- 用户、积分、主题、生成、投稿、公开展示之间如何流转。
- 哪些表是现有基础能力，哪些表是 Monica AI 新增业务能力。
- 哪些关系决定 API 鉴权、归属校验和索引设计。

## 2. 表名与中文业务名称

### 2.1 已有基础表

<CheetTable title="已有基础表">

| 表名 / Model | 中文业务名称 | 当前职责 |
| --- | --- | --- |
| `User` | 用户 | 用户身份主表，承接匿名用户、注册用户、Clerk 用户、Stripe Customer |
| `Credit` | 积分余额 | 用户当前可用积分余额，区分免费、订阅、一次性购买积分 |
| `CreditAuditLog` | 积分流水 | 记录积分发放、消耗、调整、清空等审计流水 |
| `Subscription` | 订阅 | 用户订阅状态和订阅周期 |
| `Transaction` | 订单交易 | Stripe 等支付订单、付款状态、授予积分 |
| `UserBackup` | 用户备份 | 用户删除或迁移时的备份记录 |
| `Apilog` | API 日志 | 外部系统或关键 API 调用日志 |

</CheetTable>

### 2.2 Monica AI 新增业务表

<CheetTable title="Monica AI 新增业务表">

| 表名 / Model | 中文业务名称 | 当前职责 |
| --- | --- | --- |
| `ThemeSubmission` | 主题提交 | 用户提交每日主题想法，保存原始文本、当前审核状态和 reviewFlow 审批流节点 |
| `Theme` | 正式主题 | 管理员创建或采纳后的每日主题，保存展示内容、SEO、生成器灵感、发布日期和精选图片 |
| `ReferenceImage` | 参考图 | 用户本地上传后的参考图片 R2 定位信息，作为生成输入素材 |
| `GenerationJob` | 生成任务 | 图片生成的异步任务记录 |
| `GeneratedImage` | 生成图片 | AI 生成出的用户私有图片资产 |
| `ImageSubmission` | 作品投稿 | 用户将生成图片提交到主题的候选作品，保存当前审核状态和 reviewFlow 审批流节点 |
| `PublicImage` | 公开图片 | 审核通过后进入 Explore / Theme Gallery 的公开展示记录 |
| `ImageLike` | 图片点赞 | 用户对公开图片的点赞关系 |
| `ImageSave` | 图片收藏 | 用户对公开图片的收藏关系 |

</CheetTable>

## 3. 总体数据流向图

本节箭头表示业务数据流向和查询归因，不等价于 Prisma relation。Monica AI 业务表之间不声明 Prisma relation，只保存 scalar id 和索引；列表页需要扩展展示信息时，由 repository/service 按 id 集合显式查询并组装。来源追溯字段只作为标量查询条件使用。

<Mermaid
  title="3. 总体数据流向图"
  chart={`
flowchart TD
  User[User-用户]
  Credit[Credit-积分余额]
  CreditAuditLog[CreditAuditLog-积分流水]
  Subscription[Subscription-订阅]
  Transaction[Transaction-订单交易]

  ThemeSubmission[ThemeSubmission-主题提交]
  Theme[Theme-正式主题]
  ReferenceImage[ReferenceImage-参考图]
  GenerationJob[GenerationJob-生成任务]
  GeneratedImage[GeneratedImage-生成图片]
  ImageSubmission[ImageSubmission-作品投稿]
  PublicImage[PublicImage-公开图片]
  ImageLike[ImageLike-图片点赞]
  ImageSave[ImageSave-图片收藏]

  User --> Credit
  User --> CreditAuditLog
  User --> Subscription
  User --> Transaction

  Transaction --> Credit
  Transaction --> CreditAuditLog

  User --> ThemeSubmission
  ThemeSubmission --> Theme

  User --> ReferenceImage

  User --> GenerationJob
  Theme --> GenerationJob
  ReferenceImage --> GenerationJob
  GenerationJob --> CreditAuditLog

  GenerationJob --> GeneratedImage
  User --> GeneratedImage

  GeneratedImage --> ImageSubmission
  User --> ImageSubmission
  Theme --> ImageSubmission

  ImageSubmission --> PublicImage
  GeneratedImage --> PublicImage
  Theme --> PublicImage
  User --> PublicImage

  PublicImage --> ImageLike
  User --> ImageLike
  PublicImage --> ImageSave
  User --> ImageSave
`}
/>

## 4. 主业务链路

### 4.1 主题生产链路

<Mermaid
  title="4.1 主题生产链路"
  chart={`
flowchart LR
  User[User-用户] --> ThemeSubmission[ThemeSubmission-主题提交]
  ThemeSubmission --> Review[审核/编辑]
  Review --> Theme[Theme-正式主题]
  ThemeSubmission --> CreditAuditLog[CreditAuditLog-积分流水]
`}
/>

说明：

- 普通用户通过 `ThemeSubmission-主题提交` 提交主题文本想法。
- 管理员接受用户提交后创建 `Theme-正式主题`，也可以直接创建 `Theme-正式主题`。
- `ThemeSubmission` 不保存正式主题字段；`Theme.sourceSubmissionId` 指向来源提交的数字 `id`，Theme 字段可从提交内容初始化但后续独立维护。
- `Theme-正式主题` 表示管理员可管理、可排期、可用于前台展示和投稿目标的主题。
- 用户提交主题获得奖励时，不新建奖励表，写入 `CreditAuditLog-积分流水`。

### 4.2 图片生成链路

<Mermaid
  title="4.2 图片生成链路"
  chart={`
flowchart LR
  User[User-用户] --> GenerationJob[GenerationJob-生成任务]
  Theme[Theme-正式主题] --> GenerationJob
  User --> ReferenceImage[ReferenceImage-参考图]
  ReferenceImage --> GenerationJob[GenerationJob-生成任务]
  GenerationJob --> CreditAuditLog[CreditAuditLog-积分流水]
  GenerationJob --> GeneratedImage[GeneratedImage-生成图片]
`}
/>

说明：

- Assistant 中间过程不持久化；最终用于生成的 prompt 记录在 `GenerationJob-生成任务`。
- `ReferenceImage-参考图` 是输入素材，不是作品；表内只保存 `cdnImagePrefix`、`storageKey` 等 R2 定位信息，最终展示 URL 由业务代码拼接。
- `GenerationJob-生成任务` 是调用 AI、记录供应商状态和预计/实际积分消耗的任务表。
- `GeneratedImage-生成图片` 是 Studio 中展示的用户私有图片资产；图片 URL 解析规则为 `sourceImageUrl` 优先，否则按 CDN base、`cdnImagePrefix`、`storageKey` 拼接。
- 生成任务创建时预扣减积分；失败、拦截或取消时由积分服务按 `GenerationJob.jobId` 返还。扣费和返还通过 `CreditAuditLog-积分流水` 追溯，余额仍由 `Credit-积分余额` 管理。

### 4.3 投稿与公开展示链路

<Mermaid
  title="4.3 投稿与公开展示链路"
  chart={`
flowchart LR
  User[User-用户] --> GeneratedImage[GeneratedImage-生成图片]
  GeneratedImage --> ImageSubmission[ImageSubmission-作品投稿]
  Theme[Theme-正式主题] --> ImageSubmission
  ImageSubmission --> PublicImage[PublicImage-公开图片]
  GeneratedImage --> PublicImage
  PublicImage --> ImageLike[ImageLike-图片点赞]
  PublicImage --> ImageSave[ImageSave-图片收藏]
`}
/>

说明：

- 生成图片不会自动公开。
- 用户主动提交后创建 `ImageSubmission-作品投稿`。
- 投稿审核过程写入 `ImageSubmission.reviewFlow`。
- 通过审核后创建 `PublicImage-公开图片`，并由 `PublicImage.sourceSubmissionId` 记录来源投稿；`ImageSubmission` 不反向关联公开图片。
- `PublicImage-公开图片` 不重复存图片文件，它引用 `GeneratedImage-生成图片`，只承载公开展示、排序、点赞、收藏相关数据。

### 4.4 支付与积分链路

<Mermaid
  title="4.4 支付与积分链路"
  chart={`
flowchart LR
  User[User-用户] --> Transaction[Transaction-订单交易]
  Transaction --> Subscription[Subscription-订阅]
  Transaction --> Credit[Credit-积分余额]
  Transaction --> CreditAuditLog[CreditAuditLog-积分流水]
  GenerationJob[GenerationJob-生成任务] --> CreditAuditLog
  ThemeSubmission[ThemeSubmission-主题提交] --> CreditAuditLog
`}
/>

说明：

- 购买、订阅、支付状态继续使用现有 `Transaction-订单交易` 和 `Subscription-订阅`。
- 用户当前余额继续使用 `Credit-积分余额`。
- 所有积分变动继续使用 `CreditAuditLog-积分流水`。
- 图片生成预扣减、最终消耗或失败返还时，`CreditAuditLog.operationReferId` 可记录 `GenerationJob.jobId`。
- 主题提交奖励积分时，`CreditAuditLog.operationReferId` 可记录 `ThemeSubmission.id`。

## 5. 核心关系清单

### 5.1 用户关系

下表描述业务归属关系，不表示 Prisma 必须在 `User` Model 上声明反向 relation。Monica AI 业务表通过 `userId` 标量字段做权限校验和索引查询；需要用户展示信息时由 service 显式查询 `User`。

<CheetTable title="用户关系">

| 来源 | 关系 | 目标 | 说明 |
| --- | --- | --- | --- |
| `User-用户` | 1:1 | `Credit-积分余额` | 一个用户一份积分余额 |
| `User-用户` | 1:1 | `Subscription-订阅` | 一个用户当前一份订阅状态 |
| `User-用户` | 1:N | `Transaction-订单交易` | 一个用户可有多笔订单 |
| `User-用户` | 1:N | `CreditAuditLog-积分流水` | 一个用户有多条积分流水 |
| `User-用户` | 1:N | `ThemeSubmission-主题提交` | 用户可提交多个主题想法 |
| `User-用户` | 1:N | `GenerationJob-生成任务` | 用户可创建多个生成任务 |
| `User-用户` | 1:N | `GeneratedImage-生成图片` | 用户可拥有多张私有生成图 |
| `User-用户` | 1:N | `ImageSubmission-作品投稿` | 用户可提交多个作品 |
| `User-用户` | 1:N | `PublicImage-公开图片` | 用户可发布多个公开作品 |
| `User-用户` | 1:N | `ImageLike-图片点赞` | 用户可点赞多张公开图片 |
| `User-用户` | 1:N | `ImageSave-图片收藏` | 用户可收藏多张公开图片 |

</CheetTable>

### 5.2 主题关系

<CheetTable title="主题关系">

| 来源 | 关系 | 目标 | 说明 |
| --- | --- | --- | --- |
| `ThemeSubmission-主题提交` | N:1 | `Theme-正式主题` | 用户提交被接受后关联正式主题；正式主题字段独立维护 |
| `Theme-正式主题` | 1:N | `GenerationJob-生成任务` | 围绕主题生成的任务记录主题 |
| `Theme-正式主题` | 1:N | `ImageSubmission-作品投稿` | 用户把作品提交到某个主题 |
| `Theme-正式主题` | 1:N | `PublicImage-公开图片` | 主题 Gallery 展示公开图片 |

</CheetTable>

### 5.3 创作和生成关系

<CheetTable title="创作和生成关系">

| 来源 | 关系 | 目标 | 说明 |
| --- | --- | --- | --- |
| `ReferenceImage-参考图` | 1:N | `GenerationJob-生成任务` | 一个参考图可被多个生成任务使用 |
| `GenerationJob-生成任务` | 1:N | `GeneratedImage-生成图片` | 一个任务可生成 1/2/4 张图片 |

</CheetTable>

### 5.4 投稿和公开关系

<CheetTable title="投稿和公开关系">

| 来源 | 关系 | 目标 | 说明 |
| --- | --- | --- | --- |
| `GeneratedImage-生成图片` | 1:N | `ImageSubmission-作品投稿` | 一张图理论上可提交到不同主题，业务上可限制重复提交 |
| `ImageSubmission-作品投稿` | 来源追溯 | `PublicImage-公开图片` | 投稿审核通过后创建公开展示记录，公开记录用 `sourceSubmissionId` 追溯来源，不声明反向 relation |
| `GeneratedImage-生成图片` | 1:1 | `PublicImage-公开图片` | 公开图片引用原始生成图 |
| `PublicImage-公开图片` | 1:N | `ImageLike-图片点赞` | 公开图片可被多个用户点赞 |
| `PublicImage-公开图片` | 1:N | `ImageSave-图片收藏` | 公开图片可被多个用户收藏 |

</CheetTable>

## 6. 权限边界从关系中推导

<CheetTable title="权限边界从关系中推导">

| 表名 / Model | 读权限 | 写权限 | 原因 |
| --- | --- | --- | --- |
| `User-用户` | 本人 / 系统 | 系统 | 用户身份基础表 |
| `Credit-积分余额` | 本人 / 系统 | 系统 | 余额不能由业务接口直接写 |
| `CreditAuditLog-积分流水` | 本人 / 系统 | 系统 | 积分审计流水 |
| `Transaction-订单交易` | 本人 / 系统 | 支付系统 / 系统 | 支付订单 |
| `Subscription-订阅` | 本人 / 系统 | 支付系统 / 系统 | 订阅状态 |
| `ThemeSubmission-主题提交` | 本人 / 管理员 | 本人提交，管理员审核 | 用户提交主题想法 |
| `Theme-正式主题` | 公开 | 管理员 / 系统 | 公开主题内容 |
| `ReferenceImage-参考图` | 本人 | 本人 / 系统 | 用户上传的私有输入素材 |
| `GenerationJob-生成任务` | 本人 | 本人创建，系统更新 | 用户私有异步任务 |
| `GeneratedImage-生成图片` | 本人 | 系统 | 用户私有图片资产 |
| `ImageSubmission-作品投稿` | 本人 / 管理员 | 本人提交，管理员审核 | 投稿审核对象 |
| `PublicImage-公开图片` | 公开 | 系统 / 管理员 | 公开展示记录 |
| `ImageLike-图片点赞` | 本人状态 / 公开计数 | 本人 | 用户互动 |
| `ImageSave-图片收藏` | 本人状态 | 本人 | 用户收藏 |

</CheetTable>

## 7. 关键设计结论

- `User-用户` 是所有业务数据归属的根，但 Monica AI 业务表不在 `User` Model 上维护反向 relation。
- `Credit-积分余额`、`CreditAuditLog-积分流水`、`Transaction-订单交易`、`Subscription-订阅` 已经完备，不需要为 Monica AI 新建积分或订单表。
- `ThemeSubmission-主题提交` 是用户主题想法来源，正式运营字段不存放在提交表。
- `Theme-正式主题` 是创作上下文和投稿目标。
- `GenerationJob-生成任务` 是 AI 供应商调用、异步状态、预计消耗和实际消耗记录的核心任务表。
- `GeneratedImage-生成图片` 是 My Studio 的核心资产。
- `ImageSubmission-作品投稿` 是从私有图进入公开展示前的审核层。
- `PublicImage-公开图片` 是公开展示投影，不重复存储图片资产。
- `ReferenceImage-参考图` 是生成输入素材，不是作品。
- `ImageLike-图片点赞` 和 `ImageSave-图片收藏` 是用户与公开图片之间的关系表。
