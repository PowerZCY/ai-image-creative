---
title: Monica AI 数据流转时序图
description: Monica AI 业务场景的数据表流转、同步异步边界、Redis 锁与积分预扣减设计
date: 2026-06-11
---

## 1. 说明

本文按真实用户功能流程说明数据如何流转。这里的“场景”不是单个原子操作，而是用户完成一个实际功能的完整过程。

不覆盖：

- 用户注册 / 登录。
- 购买积分。
- 订单支付。
- 订阅管理。

特别规则：

- 凡是调用 AI 的地方，都必须先进入 Redis 分布式锁。
- 凡是调用 AI 且消耗积分的地方，都必须执行积分预扣减。
- 积分预扣减操作的是现有 `Credit-积分余额` 和 `CreditAuditLog-积分流水`。
- AI 异步任务失败、被安全策略拦截、供应商无结果或超时时，必须由积分服务完成返还，最终业务语义是不消耗积分。

## 2. 同步与异步标记

| 标记 | 含义 |
| --- | --- |
| 同步 | 用户请求内立即完成，接口返回前完成 |
| 异步 | 接口先返回任务或状态，后续由 Worker / Queue / Provider callback 完成 |
| 锁 | 使用 Redis 分布式锁保护并发 |
| 预扣减 | AI 调用前先扣减或占用积分 |
| 返还 | AI 失败后由积分服务按 `operationReferId` 幂等返还预扣减积分 |

## 3. 场景 A：用户提交主题想法

用户在 `Submit a theme` 页面提交每日主题想法。该流程只提交文本，不上传说明图片，也不填写 SEO、tags 或生成器灵感。提交后进入审核，普通用户不能修改，只能查看状态和 Notes。

涉及数据表：

- `User-用户`
- `ThemeSubmission-主题提交`
- `Credit-积分余额`
- `CreditAuditLog-积分流水`

<Mermaid
  title="3. 场景 A：用户提交主题想法"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant API as ThemeSubmission API
  participant DB as 数据库
  participant Credit as 积分服务

  FE->>API: 提交主题想法(title/details/submitReason?)
  API->>DB: 同步创建 ThemeSubmission(status=under_review)

  alt 用户首次提交主题想法
    API->>Credit: 同步增加 +20 credits
    API->>DB: 同步写入 CreditAuditLog(operationReferId=ThemeSubmission.id)
    API->>DB: 追加 ThemeSubmission.reviewFlow 奖励节点
  end
  API-->>FE: 返回提交成功、Under review 和 Notes
`}
/>

关键状态变化：

- `ThemeSubmission.status`: `under_review -> accepted / not_selected`
- `CreditAuditLog.operationReferId`: `ThemeSubmission.id`

关键约束：

- 主题提交只接收 `title`、`details`、`submitReason?`。
- `ThemeSubmission` 不保存正式主题字段，也不上传说明图。
- 提交后普通用户不可修改。
- 用户侧只展示 `Under review`、`Accepted`、`Not selected`。

## 4. 场景 B：管理员采纳主题并发布每日主题

管理员可以接受用户提交并创建正式主题，也可以在后台直接创建正式主题。`ThemeSubmission` 保存用户原始提交，`Theme` 保存正式主题的全部可运营字段，两者不共享字段状态。

涉及数据表：

- `User-用户`
- `ThemeSubmission-主题提交`
- `Theme-正式主题`
- `Credit-积分余额`
- `CreditAuditLog-积分流水`

<Mermaid
  title="4. 场景 B：管理员采纳主题并发布每日主题"
  chart={`
sequenceDiagram
  autonumber
  participant Admin as 管理员前端
  participant API as Admin Theme API
  participant DB as 数据库
  participant Credit as 积分服务

  Admin->>API: Accept as theme / Edit and accept
  API->>DB: 同步读取 ThemeSubmission-主题提交
  API->>DB: 同步校验 status=under_review 且未被采纳
  API->>DB: 同步创建 Theme(sourceSubmissionId=ThemeSubmission.id)
  API->>DB: 同步更新 ThemeSubmission(status=accepted)
  API->>DB: 追加 ThemeSubmission.reviewFlow 审核/奖励/主题关联节点

  alt 主题被采纳需要奖励积分
    API->>Credit: 同步增加 +300 credits
    API->>DB: 同步写入 CreditAuditLog(operationReferId=ThemeSubmission.id)
  end

  API-->>Admin: 返回正式 Theme，进入 Manage themes
`}
/>

关键状态变化：

- `ThemeSubmission.status`: `under_review -> accepted`
- `Theme.sourceSubmissionId`: 关联 `ThemeSubmission.id`
- `Theme.publishDate`: 为空表示未排期，有日期表示可按日期展示。

关键约束：

- 普通用户不能直接创建 `Theme-正式主题`。
- 图片投稿不会创建 `Theme-正式主题`。
- 管理员直接创建主题时写入 `Theme`，不需要 `ThemeSubmission`。

## 5. 场景 C：用户在首页自由生成图片

用户在 Home 输入 prompt，选择模型、风格、比例和数量，点击 Generate。该流程调用 AI，需要 Redis 分布式锁、积分预扣减和异步任务状态轮询。

涉及数据表：

- `User-用户`
- `Credit-积分余额`
- `CreditAuditLog-积分流水`
- `GenerationJob-生成任务`
- `GeneratedImage-生成图片`

<Mermaid
  title="5. 场景 C：用户在首页自由生成图片"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant API as Generation API
  participant Redis as Redis分布式锁
  participant Credit as 积分服务
  participant DB as 数据库
  participant Queue as 任务队列
  participant Worker as AI Worker
  participant Safety as SafetyService
  participant AI as AI Provider

  FE->>API: 点击 Generate(prompt/settings)
  API->>Redis: 锁 userId + feature + requestKey
  Redis-->>API: 获取锁成功

  API->>DB: 同步创建 GenerationJob-生成任务(status=pending, generationType=text_to_image)
  API->>Credit: 同步检查并预扣减 Credit-积分余额
  Credit->>DB: 同步写入 CreditAuditLog-积分流水(operationType=consume/reserve, operationReferId=jobId)
  API->>DB: 同步更新 GenerationJob(status=queued, estimatedCredits)
  API->>Queue: 异步投递 jobId
  API->>Redis: 释放锁
  API-->>FE: 返回 jobId 和 queued 状态

  loop 前端轮询任务状态
    FE->>API: GET GenerationJob(jobId)
    API->>DB: 查询 GenerationJob 和 GeneratedImage
    API-->>FE: 返回 queued/running/succeeded/failed/blocked
  end

  Worker->>DB: 异步读取 GenerationJob-生成任务
  Worker->>Safety: checkGenerationRequest(prompt/settings)
  Safety-->>Worker: passed/skipped/blocked
  Worker->>DB: 异步更新 GenerationJob(status=running)
  Worker->>AI: 调用 AI 生成图片
  AI-->>Worker: 返回图片结果
  Worker->>Safety: checkProviderResult(provider response)
  Safety-->>Worker: passed/skipped/blocked
  Worker->>DB: 异步创建 GeneratedImage-生成图片
  Worker->>DB: 异步更新 GenerationJob(status=succeeded, chargedCredits=estimatedCredits)
`}
/>

失败返还：

<Mermaid
  title="5. 场景 C：用户在首页自由生成图片"
  chart={`
sequenceDiagram
  autonumber
  participant Worker as AI Worker
  participant DB as 数据库
  participant Credit as 积分服务
  alt SafetyService 在调用 AI 前 blocked
    Worker->>DB: 更新 GenerationJob(status=blocked, chargedCredits=0)
    Worker->>Credit: 按 jobId 幂等返还预扣减积分
  else Provider 明确安全拒绝
    Worker->>DB: 更新 GenerationJob(status=blocked, failureCode=provider_rejected, chargedCredits=0)
    Worker->>Credit: 按 jobId 幂等返还预扣减积分
  else AI Provider 调用前失败
    Worker->>DB: 更新 GenerationJob(status=failed, chargedCredits=0)
    Worker->>Credit: 按 jobId 幂等返还预扣减积分
  else AI Provider 无可交付结果或超时
    Worker->>DB: 更新 GenerationJob(status=failed, failureCode, chargedCredits=0)
    Worker->>Credit: 按 jobId 幂等返还预扣减积分
  end
`}
/>

关键状态变化：

- `GenerationJob.status`: `pending -> queued -> running -> succeeded / failed / blocked`
- `GenerationJob.estimatedCredits`: 创建任务时记录预计消耗，用于展示和对账
- `GenerationJob.chargedCredits`: 成功时记录实际消耗；失败、拦截或取消时为 `0`
- `GeneratedImage.status`: `generated`
- `CreditAuditLog.operationReferId`: `GenerationJob.jobId`
- `SafetyService` 是固定扩展入口；MVP 可返回 `skipped` 或轻量 `passed`，后续可替换为自研 NSFW 审查或第三方审核
- `SafetyService.status = skipped` 表示当前阶段未启用复杂审查，不阻断流程；`blocked` 或 `provider_rejected` 才会终止任务并返还积分

## 6. 场景 D：用户使用主题灵感后生成图片

用户在 Theme Detail 或 Studio 中选择一个主题，点击 `Get ideas`，使用某个 idea 填入 prompt，再点击 Generate。

该流程有两个 AI 触点：

- Assistant 生成 ideas。
- 图片生成。

当前版本 Assistant 中间过程不持久化，ideas 只作为前端临时状态。只有用户最终点击 Generate 的 prompt 写入 `GenerationJob`。如果后续 Assistant 需要收费，应单独设计 Assistant 任务或脱敏日志，不复用图片生成任务。

涉及数据表：

- `User-用户`
- `Theme-正式主题`
- `Credit-积分余额`
- `CreditAuditLog-积分流水`
- `GenerationJob-生成任务`
- `GeneratedImage-生成图片`

<Mermaid
  title="6. 场景 D：用户使用主题灵感后生成图片"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant AssistantAPI as Assistant API
  participant GenAPI as Generation API
  participant Redis as Redis分布式锁
  participant Credit as 积分服务
  participant DB as 数据库
  participant Queue as 任务队列
  participant AI as AI Provider

  FE->>AssistantAPI: 点击 Get ideas(themeId)
  AssistantAPI->>DB: 读取 Theme(title/brief/themeNote/generatorIdeas/avoidCliches)
  AssistantAPI->>AI: 调用 AI 生成 ideas
  AI-->>AssistantAPI: 返回 ideas
  AssistantAPI-->>FE: 返回 ideas

  FE->>FE: 用户点击 Use，前端填入 prompt
  FE->>GenAPI: 点击 Generate(prompt, themeId)
  GenAPI->>Redis: 锁 userId + generation + requestKey
  GenAPI->>DB: 同步创建 GenerationJob(themeId, prompt)
  GenAPI->>Credit: 同步预扣减 Credit-积分余额
  Credit->>DB: 同步写入 CreditAuditLog(operationReferId=jobId)
  GenAPI->>Queue: 异步投递 jobId
  GenAPI->>Redis: 释放锁
  GenAPI-->>FE: 返回 jobId，前端轮询任务状态
`}
/>

关键状态变化：

- `GenerationJob.themeId`: 当前主题
- `GenerationJob.prompt`: 用户最终确认后的 prompt
- `GeneratedImage` 不保存主题字段；主题上下文保留在 `GenerationJob.themeId`，投稿时写入 `ImageSubmission.themeId`。

## 7. 场景 E：用户 Improve Prompt 后生成图片

用户输入一个粗略 prompt，点击 `Improve prompt`，确认替换或追加后再生成图片。

涉及数据表：

- `User-用户`
- `Credit-积分余额`
- `CreditAuditLog-积分流水`
- `GenerationJob-生成任务`
- `GeneratedImage-生成图片`

<Mermaid
  title="7. 场景 E：用户 Improve Prompt 后生成图片"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant AssistantAPI as Assistant API
  participant GenAPI as Generation API
  participant Redis as Redis分布式锁
  participant Credit as 积分服务
  participant DB as 数据库
  participant AI as AI Provider
  participant Queue as 任务队列

  FE->>AssistantAPI: 点击 Improve prompt(originalPrompt)
  AssistantAPI->>AI: 调用 AI 改写 prompt
  AI-->>AssistantAPI: 返回 improvedPrompt
  AssistantAPI-->>FE: 返回 original/improved

  FE->>FE: 用户点击 Replace prompt 或 Append details
  FE->>GenAPI: 点击 Generate(finalPrompt)
  GenAPI->>Redis: 锁 userId + generation + requestKey
  GenAPI->>DB: 同步创建 GenerationJob(prompt=finalPrompt)
  GenAPI->>Credit: 同步预扣减 Credit-积分余额
  Credit->>DB: 同步写入 CreditAuditLog(operationReferId=jobId)
  GenAPI->>Queue: 异步投递 jobId
  GenAPI->>Redis: 释放锁
  GenAPI-->>FE: 返回 jobId，前端轮询任务状态
`}
/>

关键状态变化：

- `GenerationJob.prompt`: 用户最终确认后的 prompt
- Improve Prompt 的 original/improved 不持久化。

## 8. 场景 F：用户上传参考图并生成图片

用户添加 reference image，然后基于 prompt 和参考图生成图片。

涉及数据表：

- `User-用户`
- `ReferenceImage-参考图`
- `GenerationJob-生成任务`
- `GeneratedImage-生成图片`
- `Credit-积分余额`
- `CreditAuditLog-积分流水`

<Mermaid
  title="8. 场景 F：用户上传参考图并生成图片"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant UploadAPI as Reference Image API
  participant GenAPI as Generation API
  participant Redis as Redis分布式锁
  participant DB as 数据库
  participant Credit as 积分服务
  participant R2 as R2存储
  participant Queue as 任务队列
  participant Worker as AI Worker
  participant Safety as SafetyService
  participant AI as AI Provider

  FE->>UploadAPI: 上传参考图
  UploadAPI->>R2: 上传原始参考图
  R2-->>UploadAPI: 返回 cdnImagePrefix 和 storageKey
  UploadAPI->>DB: 同步创建 ReferenceImage(cdnImagePrefix, storageKey, safetyStatus=pending)
  UploadAPI-->>FE: 返回 referenceId 和按业务规则拼接的预览 URL

  UploadAPI->>Safety: checkReferenceImages(reference image)
  Safety-->>UploadAPI: passed/skipped/blocked
  UploadAPI->>DB: 更新 ReferenceImage.safetyStatus=passed/skipped/failed

  FE->>GenAPI: 点击 Generate(prompt, referenceId)
  GenAPI->>Redis: 锁 userId + generation + requestKey
  GenAPI->>DB: 同步校验 ReferenceImage 归属和 safetyStatus
  GenAPI->>DB: 同步创建 GenerationJob(referenceId)
  GenAPI->>Credit: 同步预扣减 Credit-积分余额
  Credit->>DB: 同步写入 CreditAuditLog(operationReferId=jobId)
  GenAPI->>Queue: 异步投递 jobId
  GenAPI->>Redis: 释放锁
  GenAPI-->>FE: 返回 jobId，前端轮询任务状态

  Worker->>DB: 读取 GenerationJob 和 ReferenceImage
  Worker->>Safety: checkGenerationRequest(prompt/referenceId/settings)
  Safety-->>Worker: passed/skipped/blocked
  Worker->>R2: 基于 storageKey 生成可被 Provider 访问的 public/signed view URL
  Worker->>AI: 异步调用 AI，携带 prompt 和 reference image URL
  AI-->>Worker: 返回 message.images[]，可能是 base64 data URL
  loop 每张生成图
    Worker->>R2: 将生成图片二进制上传到 R2
    R2-->>Worker: 返回 cdnImagePrefix 和 storageKey
    Worker->>Safety: checkProviderResult(图片 URL/provider response)
    Safety-->>Worker: passed/skipped/blocked
    Worker->>DB: 创建可交付 GeneratedImage(providerImageIndex, sourceImageUrl?, cdnImagePrefix?, storageKey?)
  end
  Worker->>DB: 更新 GenerationJob(status=succeeded, chargedCredits=estimatedCredits)
`}
/>

关键状态变化：

- `ReferenceImage.safetyStatus`: `pending -> passed / skipped / failed`
- `GenerationJob.referenceId`: 关联参考图
- `GeneratedImage` 是结果作品，`ReferenceImage` 仍只是输入素材
- QStash 任务消息只传 `jobId`，不传 reference image URL 或 base64 图片
- OpenRouter 输出如果是公网 URL，先保存到 `GeneratedImage.sourceImageUrl`；如果是 base64 data URL 或已完成迁移，则保存 `cdnImagePrefix` 和 `storageKey`
- 图片展示规则统一为：`sourceImageUrl` 不为空则直接使用；为空则按 CDN base、`cdnImagePrefix` 和 `storageKey` 拼接
- MVP 阶段 `SafetyService` 可以只做文件基础校验并返回 `skipped`；后续接入图片 NSFW 审查时不需要改变上传和生成主流程
- `SafetyService.status = skipped` 不阻断流程；`blocked` 会让生成任务进入 `blocked` 并触发积分返还

## 10. 场景 H：用户提交作品到主题

用户从生成结果或 Studio 中点击 `Submit to this theme`，提交作品进入审核。该流程只处理作品投稿，不创建主题提案，也不创建正式主题。该流程不调用 AI 时可以同步；如果启用主题相关性 AI 审核，则审核部分必须走 Redis 锁和积分策略。如果主题相关性审核不收费，也可以只加锁不扣积分。

涉及数据表：

- `User-用户`
- `GeneratedImage-生成图片`
- `Theme-正式主题`
- `ImageSubmission-作品投稿`
- `PublicImage-公开图片`

<Mermaid
  title="10. 场景 H：用户提交作品到主题"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant API as Submission API
  participant DB as 数据库
  participant Review as 审核服务
  participant Redis as Redis分布式锁
  participant AI as AI Provider

  FE->>API: 提交作品(imageId, themeId, title, creationNote?)
  API->>DB: 同步校验 GeneratedImage 归属
  API->>DB: 同步校验 Theme 为正式主题且可投稿
  API->>DB: 同步创建 ImageSubmission(status=submitted, promptSnapshot=metadata)
  API->>DB: 同步更新 GeneratedImage(status=submitted, isLocked=true)
  API-->>FE: 返回 Submitted

  API->>Review: 异步触发审核
  Review->>DB: 追加 ImageSubmission.reviewFlow(safety)

  opt 使用 AI 做主题相关性审核
    Review->>Redis: 锁 ImageSubmission.id + relevance_check
    Review->>AI: 调用 AI 判断主题相关性
    AI-->>Review: 返回 relevance result
    Review->>Redis: 释放锁
  end

  Review->>DB: 追加 ImageSubmission.reviewFlow(theme_relevance)
  Review->>DB: 异步更新 ImageSubmission(status=needs_review/approved/rejected)

  alt 审核通过
    Review->>DB: 创建 PublicImage(sourceSubmissionId=ImageSubmission.id)
    Review->>DB: 追加 ImageSubmission.reviewFlow(publish)
    Review->>DB: 更新 GeneratedImage(status=approved, isLocked=true)
  else 审核拒绝
    Review->>DB: 追加 ImageSubmission.reviewFlow(reject note)
    Review->>DB: 更新 GeneratedImage(status=rejected, isLocked=false)
  end
`}
/>

关键状态变化：

- `ImageSubmission.status`: `submitted -> needs_review / approved / rejected`
- `GeneratedImage.status`: `generated -> submitted -> approved / rejected`
- `GeneratedImage.isLocked`: `submitted`、`approved` 为 `true`，`rejected` 为 `false`
- `PublicImage` 只在审核通过后创建

关键约束：

- 投稿目标是 `Theme-正式主题`，不能是 `ThemeSubmission-主题提交`。
- `ImageSubmission-作品投稿` 不改变 `ThemeSubmission-主题提交` 状态。
- 图片投稿只提交到正式 `Theme`，不会创建或修改主题提交。

## 11. 场景 I：用户浏览公开图片并点赞/收藏

用户在 Explore 或 Theme Gallery 中浏览公开图片，并进行 Like / Save。

涉及数据表：

- `User-用户`
- `PublicImage-公开图片`
- `ImageLike-图片点赞`
- `ImageSave-图片收藏`

<Mermaid
  title="11. 场景 I：用户浏览公开图片并点赞/收藏"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant API as Explore API
  participant DB as 数据库

  FE->>API: 获取公开图片列表
  API->>DB: 查询 PublicImage(deleted=0)
  API->>DB: 如果已登录，查询 ImageLike/ImageSave 当前用户状态
  API-->>FE: 返回图片列表和 liked/saved 状态

  FE->>API: 打开图片详情或图片曝光
  API->>Redis: 增加 view 计数，并按 userId/visitorId 去重增加 unique view 计数
  API-->>FE: 返回图片详情

  FE->>API: 点击 Like
  API->>DB: 同步 upsert/soft-delete ImageLike
  API->>Redis: 实时更新 like 计数
  API-->>FE: 返回 liked 状态和 likeCount

  FE->>API: 点击 Save
  API->>DB: 同步 upsert/soft-delete ImageSave
  API->>Redis: 实时更新 save 计数
  API-->>FE: 返回 saved 状态和 saveCount

  API->>DB: 异步任务批量落库 PublicImage 聚合计数
`}
/>

关键状态变化：

- `ImageLike.deleted`: `0 / 1`
- `ImageSave.deleted`: `0 / 1`
- `PublicImage.likeCount` / `saveCount` / `viewCount` / `uniqueViewCount` 是聚合计数，由 Redis 实时维护并异步落库
- `ImageLike` 和 `ImageSave` 服务于用户自己的 liked/saved 状态、去重和个人历史列表，不承担全量计数实时写入

## 12. 场景 J：用户在 My Studio 查看创作资产

用户进入 My Studio，查看自己的生成图、投稿状态，并按状态筛选。主题想法提交由独立的 `Submit a theme` 页面负责。

涉及数据表：

- `User-用户`
- `GeneratedImage-生成图片`
- `GenerationJob-生成任务`
- `ImageSubmission-作品投稿`
- `PublicImage-公开图片`

<Mermaid
  title="12. 场景 J：用户在 My Studio 查看创作资产"
  chart={`
sequenceDiagram
  autonumber
  participant FE as 前端
  participant API as Studio API
  participant DB as 数据库

  FE->>API: 打开 My Studio(status filter)
  API->>DB: 查询 GeneratedImage(userId=currentUser)
  API->>DB: 按 jobId 批量查询 GenerationJob 摘要
  API->>DB: 按 imageId 批量查询 ImageSubmission 最新状态
  API->>DB: 按 imageId 批量查询 PublicImage(deleted=0)
  API-->>FE: 返回图片列表

  FE->>API: 点击 View prompt
  API->>DB: 校验 GeneratedImage.userId
  API->>DB: 返回 GenerationJob.prompt
  API-->>FE: 返回 prompt
`}
/>

关键点：

- Studio 不查询 `ReferenceImage` 作为作品。
- Studio 的主资产是 `GeneratedImage-生成图片`。
- 投稿状态来自 `ImageSubmission-作品投稿` 和 `GeneratedImage.status`。
- Studio 顶层状态只需要 `All` 和 `Submitted`；Submitted 下展示 `Pending review`、`Approved`、`Rejected`。

## 13. AI 场景通用事务原则

所有 AI 场景统一遵循以下顺序：

<Mermaid
  title="13. AI 场景通用事务原则"
  chart={`
flowchart TD
  A[收到用户 AI 请求] --> B[Redis 分布式锁]
  B --> C[校验用户权限和资源归属]
  C --> D[估算积分成本]
  D --> E[预扣减 Credit-积分余额]
  E --> F[创建业务任务或日志]
  F --> G[投递异步任务]
  G --> H[释放 Redis 锁]
  H --> I[前端轮询任务状态]
  H --> J[Worker 调用 AI]
  J --> K{AI 是否成功且有可交付结果}
  K -->|成功| L[写入结果表并更新任务成功]
  L --> M[记录 chargedCredits=estimatedCredits]
  K -->|失败| N[记录失败原因并更新任务失败]
  N --> O[积分服务按 jobId 幂等返还预扣减积分]
`}
/>

必须保证：

- 同一个用户同一个请求不能重复扣积分。
- 预扣减和任务创建要有可追溯关联。
- `CreditAuditLog.operationReferId` 必须指向业务任务 ID。
- `GenerationJob` 不承载积分状态机，只通过 `estimatedCredits` 和 `chargedCredits` 辅助对账。
- 失败返还由积分服务保证幂等和最终成功。
- Worker 重试必须幂等。
- AI 结果写入必须幂等，避免重复创建图片。
