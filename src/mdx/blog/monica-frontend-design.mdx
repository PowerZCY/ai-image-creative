---
title: Monica AI 前端技术方案
description: Monica AI 前端页面架构、组件选型、状态管理、异步轮询、样式规则和 i18n 落地方案
date: 2026-06-13
---

## 1. 设计边界

本文基于最新产品原型 `docs/index.html`、产品说明书、数据流设计、后端技术方案，以及现有组件体系输出前端技术方案。

本文重点覆盖：

- 页面和路由结构。
- Server Component / Client Component 边界。
- 创作面板、Assistant、生成任务、图片投稿、主题提案、管理员审核、Explore、Studio 的组件拆分。
- 异步生成任务的前端轮询策略。
- 样式和交互规则。
- 与 `@windrun-huaiin/*` 底层组件的使用边界。
- i18n 文案组织。

不覆盖：

- AI Provider 参数细节。
- 后端数据库迁移。
- 管理后台通用权限、组织和系统设置设计。
- 具体图片存储供应商 UI。

## 2. 现有前端基础

当前项目已经具备以下基础能力：

- Next.js App Router。
- React 19。
- `next-intl` 多语言。
- `@windrun-huaiin/base-ui`：基础图标、主题、样式工具。
- `@windrun-huaiin/third-ui`：站点布局、Hero、按钮、弹窗、选择器、动效等通用组件。
- `@windrun-huaiin/lib`：通用工具，如 `cn`。
- `fumadocs` 风格的内容布局能力。

现有首页 Hero 已经采用以下组件组合：

```tsx
import { ZapIcon } from '@windrun-huaiin/base-ui/icons';
import { themeHeroEyesOnClass } from '@windrun-huaiin/base-ui/lib';
import { GradientButton } from '@windrun-huaiin/third-ui/main/buttons';
import { HeroMedia, HeroSection } from '@windrun-huaiin/third-ui/main/hero';
import { cn } from '@windrun-huaiin/lib/utils';
```

结论：

- Monica AI 不需要重建站点外壳、Hero、按钮、弹窗、选择器、图标和基础动效。
- 前端实现重点应放在业务组件组合、状态流转和 API 数据适配。
- 视觉风格应延续当前 `HeroSection + HeroMedia + GradientButton + themeHeroEyesOnClass` 的主题感，而不是重新做一套孤立 UI。

## 3. 页面结构

Monica AI 用户侧建议包含以下页面：

<CheetTable title="页面结构">

| 页面 | 路由建议 | 页面职责 |
| --- | --- | --- |
| Home | `/[locale]` 或 `/[locale]/create` | 通用创作入口、今日主题入口、创作结果展示 |
| Theme Detail | `/[locale]/themes/[themeId]` | 主题详情、主题上下文创作、主题 Gallery |
| Explore | `/[locale]/explore` | Themes / Images 浏览、筛选、图片详情 |
| Submit a theme | `/[locale]/submit-theme` | 用户提交纯文本主题想法、查看自己的主题提案审核状态 |
| My Studio | `/[locale]/studio` | 个人创作、生成历史、投稿状态管理 |
| Admin Review | `/[locale]/admin/monica` | 管理员审核主题提案、管理正式主题、审核图片投稿 |
| Public Image Detail | 可用弹窗或 `/[locale]/images/[publicImageId]` | 公开图片详情、Use prompt、Like、Save |

</CheetTable>

MVP 可以先把 Public Image Detail 做成 Explore 和 Theme Gallery 内的弹窗。后续如果需要 SEO 或分享，再增加独立详情页。

顶部导航以 `docs/index.html` 原型为准：

- Home。
- Explore。
- Submit a theme。
- My Studio。
- Admin，只有管理员可见。

Theme Detail 不是一级导航项，由 Today theme、Explore theme card、Theme Picker 或公开图片详情跳转进入。

## 4. 布局方案

### 4.1 根布局

继续使用现有根布局和 `DocsRootProvider` / `SiteHomeLayout` 能力。

页面组不重复配置主题。主题只在根 layout 配置：

```tsx
<DocsRootProvider
  theme={{ mode: themeMode }}
  i18n={{ locale, locales: generatedLocales, translations: fumaTranslations }}
>
  {children}
</DocsRootProvider>
```

### 4.2 Home 组布局

Monica AI 页面属于站点主体验，应继续挂在 `SiteHomeLayout` 下：

- 使用现有 Header、Footer、GoToTop。
- `floatingNav: true`。
- 导航配置继续来自 `layout.nav.tsx` 和站点配置。
- 不在业务页面内部重复实现顶栏。

### 4.3 页面内容容器

业务页面内部使用统一内容宽度：

- 常规页面：`mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8`。
- 创作工作台：允许 `max-w-[1400px]`，但必须保持移动端可用。
- 创作面板和结果区用 CSS grid，而不是嵌套多层卡片。

## 5. 视觉风格

### 5.1 总体风格

当前站点风格是：

- 暗色或主题色友好的现代 SaaS / AI 工具风格。
- 大标题 + 主题渐变强调。
- 内容区简洁，避免过度装饰。
- 使用真实媒体图或生成图作为视觉核心。
- 操作按钮圆角胶囊，重点 CTA 使用主题渐变。

Monica AI 应保持：

- 创作工具优先，避免把首屏做成纯营销页。
- 页面第一屏直接给用户可用的创作入口。
- 主题色用于重点动作和焦点状态，不做大面积单色铺满。
- 图片结果、主题封面、公开作品是视觉主体。

### 5.2 Hero 规则

Home 可以保留 Hero，但 Hero 必须服务创作入口：

- 使用 `HeroSection` 和 `HeroMedia`。
- H1 使用站点现有模式：普通标题 + `themeHeroEyesOnClass` 渐变强调。
- CTA 使用 `GradientButton`。
- Hero media 展示产品真实创作结果或主题作品，不使用抽象 SVG 装饰。
- Hero 下方应立即接创作面板，让用户无需滚动太远即可开始生成。

### 5.3 创作区视觉

创作面板是产品核心，不应该设计成营销卡片。

建议：

- 使用大面积安静 surface：`bg-background`、`bg-muted/30`、`border-border/60`。
- 主要输入区稳定高度，避免 Assistant 输出导致布局跳动。
- Toolbar 和设置项尽量密集但清晰。
- 结果图片区使用固定 aspect ratio，生成中保持 skeleton 占位。
- 图片卡片允许使用轻边框和微弱阴影，不用多层嵌套卡片。

## 6. 组件选型规则

### 6.1 按钮

遵循 `docs/skills/button-skills.mdx`。

<CheetTable title="按钮组件选择">

| 场景 | 组件 |
| --- | --- |
| 页面主 CTA，如 Start creating、Upgrade | `GradientButton` |
| Generate、Submit、Download、Use prompt 等业务动作 | `XButton type="single"` |
| 图片卡片主动作 + 更多动作 | `XButton type="split"` |
| Home / Explore tab、Studio 状态切换 | `XToggleButton` |
| 工具栏纯 icon，如关闭、移除参考图 | 原生 `button` + icon + `aria-label` |

</CheetTable>

规则：

- 一组按钮中只允许一个主视觉重点。
- 弹窗 footer 使用弹窗组件自带按钮语义，不套 `GradientButton`。
- 长文案按钮设置稳定宽度，避免 loading 或状态切换导致布局跳动。
- 纯 icon 按钮必须提供 `aria-label` 或 tooltip。

### 6.2 选择器

遵循 `docs/skills/select-skills.mdx`。

<CheetTable title="选择器组件选择">

| 场景 | 组件 |
| --- | --- |
| Model、Style、Aspect Ratio、Images | `XPillSelect mode="single"` 或 `XFormPills` |
| Explore 多标签筛选 | `XPillSelect mode="multiple"` |
| Gallery 排序：Featured / Most liked / Newest | `XToggleButton` |
| Studio 顶层状态：All / Submitted；Submitted 二级状态：Pending review / Approved / Rejected | `XToggleButton` |
| 自由输入 tags 或 negative keywords | `XTokenInput`，但 negative prompt 主体仍用 textarea |

</CheetTable>

选项结构统一：

```ts
type Option = {
  label: string;
  value: string;
};
```

`value` 必须是稳定业务值，例如 `flux_pro`、`ratio_16_9`，不要用展示文案。

### 6.3 弹窗

遵循 `docs/skills/alter-dialog-skills.mdx`。

<CheetTable title="弹窗组件选择">

| 场景 | 组件 |
| --- | --- |
| 投稿确认 | `ConfirmDialog` 或业务封装的 Submit Dialog |
| 图片详情 | 业务 Dialog，内部复用弹窗 surface 规则 |
| 删除/撤回投稿 | `ConfirmDialog type="danger"` |
| 生成失败提示 | `InfoDialog type="error"` |
| 安全拦截提示 | `InfoDialog type="warn"` |
| 放弃已编辑 prompt 或关闭未保存设置 | `ConfirmDialog emphasis="cancel"` |

</CheetTable>

规则：

- Overlay 和 X 只关闭弹窗，不触发业务动作。
- 异步 loading 绑定到具体 action。
- 投稿提交、删除、撤回这类写操作使用 `loadingActions={['confirm']}`。
- 业务错误由页面状态或 toast 处理，底层弹窗不理解业务错误。

### 6.4 图标

遵循 `docs/skills/global-icon-skills.mdx`。

规则：

- 已存在的公共 icon 可从 `@windrun-huaiin/base-ui/icons` 导入。
- Monica 业务固定 icon 应在业务侧收口，例如 `src/features/monica/icons.tsx`。
- 业务固定 icon 从 `lucide-react` 导入，再用 `createGlobalLucideIcon` 包一层。
- 配置驱动 icon 才使用 `getGlobalIcon` / `getIconElement`。
- 不因为 Monica 缺一个业务 icon 就推动底层 `base-ui` 发版。

建议业务 icon：

```tsx
import { ImagePlus, Sparkles, Wand2 } from 'lucide-react';
import { createGlobalLucideIcon } from '@windrun-huaiin/base-ui/icons';

export const ImagePlusIcon = createGlobalLucideIcon(ImagePlus, 'ImagePlusIcon');
export const SparklesIcon = createGlobalLucideIcon(Sparkles, 'SparklesIcon');
export const WandIcon = createGlobalLucideIcon(Wand2, 'WandIcon');
```

### 6.5 动效

遵循 `docs/skills/animate-skills.mdx`。

`MotionBeamFrame` 可用于：

- Home 创作面板外层的轻量 hover/focus 强调。
- 当前主题重点入口。
- 生成中面板的强调边界。

不建议：

- 每个图片卡片都常驻 beam 动效。
- 大量列表项同时 active。
- 用 beam frame 替代真实边框、背景和布局。

`slot-text` 只用于短文本状态变化：

- Copy / Copied。
- Save / Saved。
- Like count 小数字变化。
- Generate 按钮短状态。

不要用于长 prompt、Assistant 输出、图片列表批量状态。

## 7. 前端模块拆分

推荐目录按业务域拆分。列表能力单独收口，因为 Studio、Explore、主题提案、管理员审核都会用到分页、筛选、骨架屏和移动端排版。

```text
src/features/monica/
  api/
    assistant.client.ts
    generation.client.ts
    explore.client.ts
    studio.client.ts
    submission.client.ts
    theme-submission.client.ts
    admin.client.ts
    paged-client.ts
  components/
    creation-panel/
      creation-panel.tsx
      prompt-input.tsx
      assistant-actions.tsx
      assistant-output.tsx
      generation-settings.tsx
      reference-image-picker.tsx
      generation-results.tsx
    theme/
      theme-hero.tsx
      theme-card.tsx
      theme-picker-dialog.tsx
      theme-gallery.tsx
    theme-submission/
      submit-theme-page.tsx
      theme-submission-form.tsx
      my-theme-submission-list.tsx
    image/
      generated-image-card.tsx
      public-image-card.tsx
      image-detail-dialog.tsx
      submit-image-dialog.tsx
      withdraw-public-image-dialog.tsx
    list/
      paged-list-shell.tsx
      list-filters.tsx
      list-pagination.tsx
      list-skeleton.tsx
      image-list-item.tsx
      text-list-item.tsx
      review-flow-inline.tsx
      review-flow-timeline.tsx
    studio/
      studio-shell.tsx
      studio-image-grid.tsx
      studio-status-tabs.tsx
    explore/
      explore-shell.tsx
      explore-filters.tsx
    admin/
      admin-review-shell.tsx
      theme-submission-review-list.tsx
      theme-manage-table.tsx
      theme-edit-dialog.tsx
      image-submission-review-list.tsx
  hooks/
    use-generation-job-polling.ts
    use-creation-session.ts
    use-assistant-actions.ts
  copy/
    monica-copy.ts
  types.ts
  constants.ts
  icons.tsx
```

原则：

- `features/monica/components` 只放业务组件。
- `api` 层封装 fetch，不在组件里散写 endpoint 字符串。
- `hooks` 处理客户端状态、轮询和分页查询。
- `copy` 将 i18n 消息转成组件可消费的数据结构。
- `types.ts` 保持前端类型，不直接把 Prisma 类型暴露到组件。
- 主题提案组件和图片投稿组件分开维护；`ThemeSubmission` 和 `ImageSubmission` 不能混用。
- 管理员审核组件只消费专用 admin API，不复用用户侧列表 API 做越权展示。

<CheetTable title="Monica 前端组件清单">

| 组件 | 所属目录 | 主要职责 | 复用/实现策略 |
| --- | --- | --- | --- |
| `CreationPanel` | `creation-panel` | Prompt、Assistant、生成参数、参考图、Generate | Monica 自研业务组合，按钮/选择器复用底层组件 |
| `GenerationResults` | `creation-panel` | 生成中、成功图片、失败和拦截状态 | Monica 自研 |
| `ThemeCard` | `theme` | Explore Themes、Theme Picker、今日主题入口 | Monica 自研，图片和文案布局可复用 |
| `ThemePickerDialog` | `theme` | Studio 中选择正式主题 | Monica 自研，弹窗语义复用底层 Dialog |
| `GeneratedImageCard` | `image` | Studio / 生成结果中的私有图片卡片 | Monica 自研 |
| `PublicImageCard` | `image` | Explore / Theme Gallery 中的公开图片卡片 | Monica 自研 |
| `ImageDetailDialog` | `image` | 公开图片详情、Like、Save、Use prompt | Monica 自研，弹窗语义复用底层 Dialog |
| `SubmitImageDialog` | `image` | 图片投稿表单，创建 `ImageSubmission` | Monica 自研 |
| `PagedListShell` | `list` | 分页列表通用容器、loading/error/empty/pagination | Monica 自研，借鉴 `docs/question-list-client.tsx` 思路 |
| `ListFilters` | `list` | 基础筛选 + 高级筛选折叠 | Monica 自研，筛选控件复用 `XFilterPills`、`XPillSelect`、`CalendarDateRangeInput` |
| `ListPagination` | `list` | 上一页、页码输入、总页数、总数、下一页 | Monica 自研，交互借鉴 question list |
| `ListSkeleton` | `list` | 文本列表、图片列表、审核列表骨架屏 | Monica 自研，形态借鉴 `docs/question-skeleton-blocks.tsx` |
| `ImageListItem` | `list` | 带图片预览列的列表行 | Monica 自研 |
| `TextListItem` | `list` | 主题提案等纯文本列表行 | Monica 自研 |
| `ReviewFlowInline` | `list` | 列表行内展示最后审核节点 | Monica 自研 |
| `ReviewFlowTimeline` | `list` | 详情弹窗中展示完整审批流 | Monica 自研 |
| `ThemeSubmissionForm` | `theme-submission` | 独立主题提案表单 | Monica 自研 |
| `MyThemeSubmissionList` | `theme-submission` | 我的主题提案分页列表 | 基于 `PagedListShell` |
| `AdminReviewShell` | `admin` | 管理端 Themes / Image submissions 审核页面壳 | Monica 自研 |
| `ThemeSubmissionReviewList` | `admin` | 审核 `ThemeSubmission` | 基于 `PagedListShell` |
| `ThemeManageTable` | `admin` | 管理正式 `Theme` | 基于 `PagedListShell`，支持缩略图组 |
| `ImageSubmissionReviewList` | `admin` | 审核 `ImageSubmission` | 基于 `PagedListShell` + `ImageListItem` |

</CheetTable>

<CheetTable title="底层组件复用边界">

| 需求 | 优先复用 | Monica 自己实现 |
| --- | --- | --- |
| 页面主 CTA | `GradientButton` | 不自造 |
| 普通业务操作 | `XButton type="single"` / `XButton type="split"` | 只包装业务入参 |
| 视图切换 | `XToggleButton` | 只处理切换后的数据查询 |
| 单选筛选 | `XFilterPills` | 筛选布局自己实现 |
| 多选筛选 | `XPillSelect mode="multiple"` | 筛选状态自己维护 |
| 日期范围 | `CalendarDateRangeInput` | 不自造日期控件 |
| 自由标签 | `XTokenInput` | 不自造 token 输入 |
| 图标 | `@windrun-huaiin/base-ui/icons` 或 Monica icons 封装 | 业务 icon 在 Monica 收口 |
| 图片列表行 | 无通用底层组件 | `ImageListItem` |
| 审批流展示 | 无通用底层组件 | `ReviewFlowInline` / `ReviewFlowTimeline` |
| 分页 POST 查询容器 | 无通用底层组件 | `PagedListShell` / hook |

</CheetTable>

## 8. Server / Client 边界

### 8.1 Server Component

适合 Server Component：

- 页面级数据首屏读取：今日主题、主题详情、Explore 初始列表、Studio 初始列表、Submit a theme 中我的提案列表、管理员审核队列。
- 获取 `next-intl` 翻译。
- 构建结构化 copy。
- SEO metadata。

### 8.2 Client Component

必须是 Client Component：

- 创作面板。
- Prompt 输入。
- Assistant 输出交互。
- Reference image 上传和本地预览。
- Generate 按钮和任务轮询。
- 图片详情弹窗。
- 投稿弹窗。
- Like / Save。
- Studio tabs。
- Submit a theme 表单。
- 管理员审核操作、主题编辑弹窗、精选图选择。

### 8.3 数据传递

页面 Server Component 负责：

```tsx
const t = await getTranslations({ locale, namespace: 'monica' });
const copy = buildMonicaCopy(t);
const todayTheme = await getTodayTheme();
```

Client Component 接收：

```tsx
<CreationPanel
  copy={copy.creationPanel}
  initialTheme={todayTheme}
  sourcePage="home"
/>
```

不要在深层共享组件里直接调用 `getTranslations` 或写 namespace key。

## 9. 创作面板设计

`CreationPanel` 是核心复用组件，出现在 Home、Theme Detail、My Studio。

核心 props：

```ts
type CreationPanelProps = {
  sourcePage: 'home' | 'theme_detail' | 'studio' | 'explore_image_detail';
  initialTheme?: ThemeSummary | null;
  initialPrompt?: string;
  initialReferenceImage?: ReferenceImageDraft | null;
  mode?: 'free' | 'theme';
  copy: CreationPanelCopy;
};
```

内部状态：

- `prompt`
- `negativePrompt`
- `selectedModel`
- `selectedStyle`
- `selectedRatio`
- `imageCount`
- `referenceImages`
- `assistantPanel`
- `currentSessionId`
- `activeJobIds`
- `generatedImages`

组件规则：

- Prompt 输入是主控状态，Assistant 只能在用户点击 Use / Replace / Append 后修改 prompt。
- Home 默认不绑定主题，今日主题只作为灵感入口。
- Theme Detail 默认绑定当前主题。
- Studio 默认不绑定主题，用户通过 Theme Picker 后才绑定。
- Generate 时只向后端提交 `themeId`、`referenceId` 和生成参数；前端的 `sourcePage` 只用于本地分析和展示，不作为 `GenerationJob` 核心字段。
- Generate 后立即创建任务并进入轮询，不等待 Worker 完成。

## 10. 异步生成轮询

前端轮询 endpoint：

```text
GET /api/monica/generation/jobs/:jobId
```

任务状态：

- `pending`
- `queued`
- `running`
- `succeeded`
- `failed`
- `blocked`
- `cancelled`

轮询策略：

- 创建任务成功后立即把结果区置为 `queued`。
- 初始 1-2 秒轮询一次。
- 超过 30 秒后降频，例如 3-5 秒一次。
- 超过产品定义的最大等待时间后显示“仍在生成，可稍后到 Studio 查看”。
- 收到 `succeeded / failed / blocked / cancelled` 后停止轮询。

Hook 设计：

```ts
type GenerationJobView = {
  jobId: string;
  status: 'pending' | 'queued' | 'running' | 'succeeded' | 'failed' | 'blocked' | 'cancelled';
  estimatedCredits: number;
  chargedCredits: number;
  images: GeneratedImageView[];
  failureCode?: string;
  failureMessage?: string;
};

function useGenerationJobPolling(jobId: string | null, options?: {
  enabled?: boolean;
  onSuccess?: (job: GenerationJobView) => void;
  onTerminal?: (job: GenerationJobView) => void;
}) {
  // 内部用 setTimeout 递归调度，避免 setInterval 与慢请求重叠
}
```

显示规则：

- `queued/running`：显示 skeleton 或进度状态。
- `succeeded`：显示图片卡片，`chargedCredits` 应等于实际消耗。
- `failed/blocked/cancelled`：显示错误状态和重试入口；业务语义是最终不消耗积分，返还由积分服务保证。
- 不在前端实现积分返还逻辑。
- 不在前端处理 OpenRouter base64 图片输出；前端展示后端解析后的图片 URL。解析规则为 `sourceImageUrl` 不为空则直接使用，否则按 CDN base、`cdnImagePrefix` 和 `storageKey` 拼接。

## 10.1 图片上传和 R2 URL 边界

Reference image 上传属于前端交互，但存储和 Provider 可访问 URL 由后端负责。

前端职责：

- 校验文件类型和大小。
- 生成本地 object URL 做即时预览。
- 先通过 R2 Explorer SDK 或前端上传能力把文件上传到 R2。
- 调用 `POST /api/monica/reference-images` 创建 `ReferenceImage` 记录。
- 保存返回的 `referenceId` 和预览 URL。
- Generate 时只提交 `referenceId`，不提交文件二进制、base64 或 R2 signed URL。

后端职责：

- 创建 `ReferenceImage`，保存 `cdnImagePrefix`、`storageKey`、mime type 和尺寸；展示 URL 由业务代码拼接，不写入 DB。
- Worker 调用 OpenRouter 前，根据 `storageKey` 临时生成可被 Provider 访问的 public/signed view URL。
- OpenRouter 返回生成图后，后端创建 `GeneratedImage`；如果有 Provider 原始 URL，保存为 `sourceImageUrl`，迁移 R2 后保存 `cdnImagePrefix` 和 `storageKey`。

前端不能依赖：

- OpenRouter 返回的 base64 data URL。
- 临时 signed URL 的长期有效性。
- `protected.view` 这类需要站内鉴权的图片地址。

因此前端组件只关心：

```ts
type ReferenceImageDraft = {
  referenceId: string;
  previewUrl: string;
  cdnImagePrefix?: string;
  storageKey: string;
  filename: string;
  mimeType: string;
};

type GeneratedImageView = {
  imageId: string;
  sourceImageUrl?: string;
  cdnImagePrefix?: string;
  storageKey?: string;
  status: string;
  isLocked: boolean;
};
```

## 11. Assistant 交互

Assistant 是创作面板内部能力，不是独立聊天页。

动作：

- Get ideas。
- More like this。
- Improve prompt。
- Ask assistant。

组件：

- `AssistantActions`：三个快捷动作。
- `AssistantOutput`：ideas / improve / ask 的结果展示。
- `AssistantIdeaCard`：单个方向，包含 Use / More like this。
- `AskAssistantBox`：短对话输入。

规则：

- Assistant 输出必须围绕图片生成，不做开放闲聊。
- Get Ideas 不自动生成图片。
- Use 只填入 prompt。
- Improve Prompt 必须展示 Original 和 Improved，用户明确选择 Replace 或 Append。
- Assistant 如果收费，也走积分服务预扣减和失败返还；前端只展示消耗提示，不处理返还。

## 12. 图片和投稿组件

### 12.1 GeneratedImageCard

展示：

- 图片预览。
- Prompt used。
- 模型、比例、风格。
- 状态。
- Download。
- Submit。

按钮：

- Download 用 `XButton type="single"`。
- Submit 可作为重点业务动作。
- 多个低频动作可收进 `XButton type="split"`。

### 12.2 SubmitImageDialog

职责：

- 展示图片预览。
- 展示目标主题。
- 展示 prompt snapshot。
- 输入图片标题。
- 输入 creation note。
- 创建 `ImageSubmission`。

弹窗：

- 使用 Confirm 语义。
- `loadingActions={['confirm']}`。
- 提交成功后关闭弹窗，并更新 Studio / 图片卡片状态为 `submitted`。
- 如果 `GeneratedImage.isLocked=true`，禁用 Submit 和 Delete，仅展示当前审核状态或撤销申请入口。
- 只有 `GeneratedImage.status` 为 `generated` 或 `rejected` 时，用户侧可以再次操作。

### 12.3 ImageDetailDialog

展示：

- 大图。
- 作者。
- 所属主题。
- Prompt。
- 标签。
- Like / Save 当前状态。

动作：

- Use as inspiration。
- Use prompt。
- Copy prompt。
- Like。
- Save。

`Copy prompt` 和 `Save/Saved` 可使用 `slot-text` 做短文本状态动画。

## 13. 页面列表和审核流设计

Monica 的列表不直接复用 `docs/question-*` 组件，因为当前需求有图片预览列、审批流、状态锁定、管理员操作等业务差异。但列表的状态管理、筛选区排版、分页条和骨架屏可以借鉴其设计思路。

统一原则：

- 所有搜索列表必须分页查询。
- 列表查询接口使用 `POST`，查询参数放在 body，便于后续扩展复杂筛选。
- 筛选变更后回到第 1 页。
- 请求使用 `AbortController`，避免快速切换筛选时旧请求覆盖新结果。
- 基础筛选常驻，高级筛选折叠；折叠时显示已启用高级筛选数量。
- 列表骨架屏必须接近真实布局，图片型列表要有固定比例图片占位。
- 移动端不强行表格化，统一退化为卡片行：图片在上或左，文本和操作在下。

### 13.1 分页列表请求模型

```ts
type MonicaPagedRequest<TFilter> = {
  page: number;
  pageSize: number;
  sortBy?: string;
  sortDirection?: 'asc' | 'desc';
  filters: TFilter;
};

type MonicaPagedResult<TItem> = {
  items: TItem[];
  pagination: {
    page: number;
    pageSize: number;
    total: number;
    totalPages: number;
  };
};
```

列表 hook：

```ts
function useMonicaPagedList<TFilter, TItem>(options: {
  endpoint: string;
  initialFilters: TFilter;
  pageSize?: number;
}) {
  // 管理 filters、page、loading、error、items、pagination、reload
  // 请求使用 POST + JSON body + AbortController
}
```

### 13.2 列表组件类型

<CheetTable title="Monica 列表类型">

| 列表类型 | 使用页面 | 主体信息 | 特殊列/区域 | 行组件 |
| --- | --- | --- | --- | --- |
| 图片资产列表 | My Studio | 生成图片、prompt、模型、主题、状态 | 图片预览、锁定状态、投稿状态、操作区 | `ImageListItem` 或 `GeneratedImageCard` |
| 公开图片列表 | Explore Images、Theme Gallery | 公开图片、标题、主题、作者、计数 | 图片预览、Like/Save/View 计数 | `PublicImageCard` |
| 图片投稿审核列表 | Admin Image submissions | 投稿图片、标题、creation note、prompt snapshot | 图片预览、审核备注输入、Approve/Reject | `ImageListItem` + 审核操作区 |
| 主题提案列表 | Submit a theme、Admin User submissions | title、details、submitReason、状态 | `ReviewFlowInline`、奖励/审核备注 | `TextListItem` |
| 正式主题管理列表 | Admin Manage themes | title、brief、source、publishDate | featured images 缩略图组、字段完整度、管理操作 | `ThemeManageTable` / `TextListItem` |

</CheetTable>

### 13.3 筛选组件设计

筛选区参考 `docs/question-list-filters.tsx` 的布局，不照搬字段。

<CheetTable title="列表筛选设计">

| 场景 | 常驻筛选 | 高级筛选 | 推荐底层组件 |
| --- | --- | --- | --- |
| Studio 图片库 | 状态、关键词、主题 | 创建时间、模型、比例、是否锁定 | `XToggleButton`、`XFilterPills`、`CalendarDateRangeInput` |
| Explore Themes | 关键词、排序 | 标签、发布时间 | `XFilterPills`、`XPillSelect` |
| Explore Images | 排序、主题、关键词 | 标签、作者、时间 | `XFilterPills`、`XPillSelect`、`CalendarDateRangeInput` |
| My theme ideas | 状态、关键词 | 提交时间、是否已采纳 | `XFilterPills`、`CalendarDateRangeInput` |
| Admin User submissions | 状态、关键词 | 用户、提交时间、奖励状态 | `XFilterPills`、`CalendarDateRangeInput` |
| Admin Image submissions | 状态、主题、关键词 | 用户、时间、安全状态 | `XFilterPills`、`XPillSelect`、`CalendarDateRangeInput` |
| Admin Manage themes | 关键词、可见性、发布时间 | 来源、字段完整度、是否有 featured images | `XFilterPills`、`XPillSelect` |

</CheetTable>

筛选规则：

- 文本输入使用圆角输入框，带清空按钮。
- 单选筛选优先 `XFilterPills`。
- 多选筛选用 `XPillSelect mode="multiple"`。
- 日期范围用 `CalendarDateRangeInput`。
- 清空筛选使用小型原生 button + icon，不使用页面主 CTA。
- 高级筛选收起时，如果存在高级筛选条件，显示 `+N` 计数。

### 13.4 列表行排版

文本型列表参考 `docs/question-list-client.tsx` 的信息组织：标题在第一行，meta chip 紧随其后，摘要文本保持 2-3 行，操作区靠右或置底。

图片型列表新增图片预览区域：

- 桌面端：图片预览固定宽度，文本区自适应，操作区右侧对齐。
- 移动端：图片预览占满行宽或保持左侧小缩略图，操作区换到下方。
- 图片区域使用稳定 `aspect-ratio`，避免加载后布局跳动。
- 审核列表图片预览必须足够清晰，不能只给极小缩略图。

<CheetTable title="列表行信息层级">

| 区域 | 文本型行 | 图片型行 |
| --- | --- | --- |
| 主视觉 | 无，依靠标题和状态 | 图片预览 |
| 第一层 | 标题 / 主题名 | 图片标题 / prompt 摘要 |
| 第二层 | 用户、状态、时间、来源 chip | 主题、作者、状态、时间 chip |
| 第三层 | details / submitReason 摘要 | creation note / prompt snapshot |
| 审核信息 | `ReviewFlowInline` | `ReviewFlowInline` 或审核备注输入 |
| 操作区 | Accept、Reject、Edit、Open | Submit、Approve、Reject、View、Like、Save |

</CheetTable>

### 13.5 审批流组件

审批流统一读取 `reviewFlow` JSON 节点，不读取已删除的 `reviewedByUserId/reviewedAt/reason` 这类单字段。

<CheetTable title="审批流组件">

| 组件 | 使用位置 | 展示内容 | 交互 |
| --- | --- | --- | --- |
| `ReviewFlowInline` | 列表行 | 当前状态、最后节点备注、处理人、时间 | 点击可打开详情 |
| `ReviewFlowTimeline` | 详情弹窗、审核详情 | 全部节点、状态、actor、note、createdAt | 只读 |
| `ReviewNoteInput` | 管理端审核操作区 | 当前审核备注输入 | Approve/Reject 前校验 |

</CheetTable>

节点前端类型：

```ts
type ReviewFlowNode = {
  status: string;
  actorUserId?: string;
  actorName?: string;
  actorType?: 'user' | 'reviewer' | 'system' | 'company';
  note?: string;
  rewardCredits?: number;
  createdAt: string;
};
```

### 13.6 页面列表落点

<CheetTable title="页面与列表组件落点">

| 页面 | 列表组件 | 查询方式 | 备注 |
| --- | --- | --- | --- |
| Explore | `ThemeGrid`、`PublicImageCard` | POST 分页，tab 切换后重查 | Themes / Images 分开 endpoint 或 body.type |
| My Studio | `StudioImageGrid`、`ImageListItem` | POST 分页 | 按 `GeneratedImage.status/isLocked` 控制动作 |
| Submit a theme | `MyThemeSubmissionList` | POST 分页 | 提交成功后刷新第一页 |
| Admin User submissions | `ThemeSubmissionReviewList` | POST 分页 | 审核操作成功后局部刷新或 reload |
| Admin Manage themes | `ThemeManageTable` | POST 分页 | 支持编辑、发布日期、精选图入口 |
| Admin Image submissions | `ImageSubmissionReviewList` | POST 分页 | 图片预览 + 审核备注 + 审核操作 |

</CheetTable>

### 13.7 页面规则

Explore：

- 顶部 tabs：Themes / Images，用 `XToggleButton`。
- Themes 只展示正式 `Theme(deleted=0)`。
- Images 只展示 `PublicImage(deleted=0)`，图片展示 URL 来自后端 DTO，不从 `ImageSubmission` 反查。
- Like / Save 做乐观更新，但失败时回滚。
- `likeCount/saveCount/viewCount/uniqueViewCount` 是聚合计数字段，前端只展示和做乐观更新，不负责最终落库策略。

My Studio：

- 顶部保留创作面板。
- 下方是个人图片库。
- 顶层状态 tabs：All / Submitted。
- Submitted 下二级状态：Pending review / Approved / Rejected。
- Studio 主资产是 `GeneratedImage`，Reference image 不进入 Studio 图片库。
- 投稿状态来自 `GeneratedImage.status`、`GeneratedImage.isLocked` 和最新 `ImageSubmission`。
- `generated/rejected` 表示非锁定，用户可以删除或再次提交。
- `submitted/approved/withdrawal_requested/withdrawn/hidden` 表示锁定，用户不能直接删除底层图片。
- 已公开作品不能由用户直接删除；用户只能申请撤销公开，进入后续审核流。

Submit a theme：

- 独立页面，不放在 Studio 内。
- 表单字段为 Theme name、Theme details、Why are you submitting this theme。
- 主题提案是纯文本审核，不上传说明图片。
- 用户提交后创建 `ThemeSubmission(status=under_review)`。
- 提交后普通用户不能编辑，只能查看状态、管理员备注、奖励节点和后续发布信息。
- 如果提案被接受为正式主题，前端通过 `Theme.sourceSubmissionId = ThemeSubmission.id` 查询正式主题信息。

Admin Review：

- 一级队列：Themes、Image submissions。
- Themes 下分 User submissions 和 Manage themes。
- User submissions 审核 `ThemeSubmission`，支持 Reject、Accept as theme、Edit and accept。
- Manage themes 管理正式 `Theme`，包括管理员直接创建主题、编辑主题字段、设置 publish date、选择 featured images。
- Image submissions 审核 `ImageSubmission`，支持 Approve、Reject 和审核备注。
- 接受主题提案时可以用提交内容初始化正式 `Theme`，但管理员编辑后的正式主题字段独立保存。
- 图片审核通过后创建或更新 `PublicImage(deleted=0)`；拒绝时更新 `GeneratedImage(status=rejected,isLocked=false)`。

## 14. i18n 方案

遵循 `docs/skills/i18n-skills.mdx`。

推荐消息结构：

```text
messages/
  en.json
  monica/
    monica.en.json
    monica.zh.json
```

运行时 `i18n.ts` 增加 `monica` 目录白名单。

文案通过 copy builder 输出：

```ts
export type CreationPanelCopy = {
  promptPlaceholder: string;
  generate: string;
  getIdeas: string;
  improvePrompt: string;
  askAssistant: string;
  safetyHint: string;
};

export function buildMonicaCopy(t: Translator): MonicaCopy {
  return {
    creationPanel: {
      promptPlaceholder: t('creationPanel.promptPlaceholder'),
      generate: t('creationPanel.generate'),
      getIdeas: t('creationPanel.getIdeas'),
      improvePrompt: t('creationPanel.improvePrompt'),
      askAssistant: t('creationPanel.askAssistant'),
      safetyHint: t('creationPanel.safetyHint'),
    },
  };
}
```

规则：

- 组件消费 copy，不直接消费 namespace。
- 不在组件里写 `locale === 'zh'` 分支。
- 页面专属文案和复用组件文案分组存放。

## 15. API Client 约定

前端 API client 只封装请求，不写 UI 状态。分页搜索接口统一使用 `POST`，参数放入 JSON body；简单动作接口继续使用语义化 `POST`。

示例：

```ts
export async function createGenerationJob(input: CreateGenerationJobInput) {
  const res = await fetch('/api/monica/generation/jobs', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(input),
  });

  if (!res.ok) {
    throw await toApiError(res);
  }

  return res.json() as Promise<CreateGenerationJobResult>;
}
```

分页查询示例：

```ts
export async function searchAdminImageSubmissions(input: MonicaPagedRequest<AdminImageSubmissionFilters>) {
  const res = await fetch('/api/monica/admin/image-submissions/search', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    credentials: 'include',
    cache: 'no-store',
    body: JSON.stringify(input),
  });

  if (!res.ok) {
    throw await toApiError(res);
  }

  return res.json() as Promise<MonicaPagedResult<ImageSubmissionListItem>>;
}
```

错误处理：

- `INSUFFICIENT_CREDITS`：提示充值或降低图片数量。
- `PROMPT_BLOCKED`：展示安全拦截说明。
- `REFERENCE_IMAGE_BLOCKED`：提示更换参考图。
- `GENERATION_JOB_FAILED`：展示重试入口。
- `PROVIDER_UNAVAILABLE`：提示稍后重试。

核心 endpoint：

- `POST /api/monica/generation/jobs`：创建生成任务。
- `GET /api/monica/generation/jobs/:jobId`：轮询任务状态。
- `POST /api/monica/reference-images`：保存参考图记录。
- `POST /api/monica/themes/search`：正式主题分页搜索。
- `GET /api/monica/themes/:themeId`：正式主题详情，前端 URL 使用主题 id。
- `POST /api/monica/submissions`：提交生成图片进入图片审核流。
- `POST /api/monica/studio/images/search`：Studio 图片分页搜索。
- `POST /api/monica/explore/images/search`：公开图片分页搜索。
- `POST /api/monica/public-images/:publicImageId/like`：点赞切换。
- `POST /api/monica/public-images/:publicImageId/save`：收藏切换。
- `POST /api/monica/themes/my`：提交主题想法。
- `POST /api/monica/themes/my/search`：我的主题提案分页搜索。
- `POST /api/monica/admin/theme-submissions/search`：管理员主题提案审核列表。
- `POST /api/monica/admin/themes/search`：管理员正式主题管理列表。
- `POST /api/monica/admin/image-submissions/search`：管理员图片投稿审核列表。
- 管理员审核 API 使用 admin 命名空间或受权限保护的 Monica API，不能复用普通用户写接口。

## 16. 样式实现规则

### 16.1 Tailwind 和 className

- 使用 Tailwind utility class。
- 组合 class 使用 `cn`。
- 优先使用 `bg-background`、`text-foreground`、`text-muted-foreground`、`border-border` 这类主题 token。
- 不硬编码大面积颜色。
- 可以在局部使用 `themeHeroEyesOnClass` 做重点文字渐变。

### 16.2 圆角和卡片

- 按钮默认胶囊或沿用组件默认。
- 工具面板和卡片圆角保持克制，避免过度圆润。
- 不做卡片套卡片。
- 页面 section 不做漂浮卡片，重复项目才用卡片。

### 16.3 响应式

- 移动端创作面板单列。
- 桌面端创作面板和结果区可以两列或上下结构。
- Toolbar 在移动端允许横向滚动，但必须保留清晰焦点态。
- 图片网格使用稳定 `aspect-ratio`。
- 文本按钮在移动端避免溢出，必要时图标 + 短文案。

### 16.4 可访问性

- 所有输入必须有 label 或 `aria-label`。
- 纯 icon button 必须有 `aria-label`。
- Dialog 打开后焦点进入弹窗。
- 生成中状态使用 `aria-live="polite"`。
- 错误提示和安全提示不能只靠颜色表达。

## 17. 前端状态原则

状态分三层：

- URL 状态：页面主 tab、可分享的轻量视图状态。
- 页面局部状态：CreationPanel prompt、settings、Assistant 输出、弹窗开关。
- 列表查询状态：filters、page、pageSize、sort、loading、error、pagination。
- 服务端状态：GenerationJob、GeneratedImage、ThemeSubmission、ImageSubmission、PublicImage like/save。

规则：

- 不引入全局状态库，除非后续跨页面状态明显复杂化。
- 创作面板内部用 `useReducer` 管理复杂表单状态。
- 分页列表先用 `useMonicaPagedList` 局部管理；后续如果请求复用变多，再评估 query cache。
- 搜索列表参数不强依赖 URL query，复杂筛选以 POST body 为准。
- 生成任务轮询必须可取消，组件卸载时停止请求。

## 18. 实施顺序

### 18.1 第一阶段：页面骨架和组件基础

- 建立 `features/monica` 目录。
- 建立 copy builder 和消息文件。
- 建立 `PagedListShell`、`ListFilters`、`ListPagination`、`ListSkeleton`。
- 建立 `ReviewFlowInline`、`ReviewFlowTimeline`。
- 实现 Home 创作面板静态 UI。
- 接入 `HeroSection` / `HeroMedia`。
- 接入 Model / Style / Ratio / Count 选择器。

### 18.2 第二阶段：Assistant 和生成任务

- 实现 Assistant actions 和输出区。
- 实现 `POST /api/monica/generation/jobs` 调用。
- 实现 `useGenerationJobPolling`。
- 实现生成中 skeleton、成功结果、失败状态。

### 18.3 第三阶段：Explore / Theme / Studio

- Theme Detail。
- Explore Themes / Images 分页查询。
- Image Detail Dialog。
- Studio 图片库、状态 tabs、分页筛选。

### 18.4 第四阶段：主题提案和管理员审核

- 独立 `Submit a theme` 页面。
- `ThemeSubmission` 表单和我的提案分页状态列表。
- Admin Review 页面骨架。
- User submissions 分页审核流。
- Manage themes 正式主题分页管理、发布日期和精选图入口。
- Image submissions 图片预览分页审核队列。

### 18.5 第五阶段：图片投稿和互动

- SubmitImageDialog。
- Studio 中 `GeneratedImage.status/isLocked` 操作控制。
- 图片投稿审核状态展示。
- 已公开图片的撤销申请入口。
- Like / Save。
- Download。

### 18.6 第六阶段：体验增强

- SlotText 短文本状态动画。
- MotionBeamFrame 重点区域 hover/focus 动效。
- 更细的空状态、错误状态、离线或超时提示。

## 19. 待确认项

- Home 是否继续作为站点首页，还是将创作入口放到 `/create`。
- 图片生成中的最长前端等待时间。
- 生成任务超时后是否自动提示用户去 Studio 查看。
- `slot-text` 是否作为默认依赖引入，还是只在局部组件懒加载使用。
- 是否需要独立公开图片详情页用于 SEO 和分享。
- 是否允许匿名用户生成图片。
